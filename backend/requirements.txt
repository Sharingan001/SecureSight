# ─────────────────────────────────────────────────────────────────────────────
# SecureSight — Python 3.12 Requirements
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# FastAPI + Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19
websockets==14.1
slowapi==0.1.9

# ML / Deep Learning
torch>=2.5.0
torchvision>=0.16.0
timm==1.0.12

# Computer Vision
opencv-python-headless==4.10.0.84
# facenet-pytorch installed separately in Dockerfile with --no-deps
# (its torch<2.3 pin conflicts with torch>=2.5 but works fine at runtime)
mediapipe==0.10.20

# Image / Audio Processing
Pillow>=10.2.0
numpy>=1.26.0
scipy>=1.12.0
scikit-image>=0.22.0
librosa==0.10.2.post1
soundfile==0.12.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite>=0.20.0       # SQLite async driver (local dev fallback)
psycopg2-binary>=2.9.9  # Required: Celery workers + Alembic use sync psycopg2 driver
alembic==1.14.1

# Task Queue
celery[redis]==5.4.0
redis==5.2.1

# Storage
minio==7.2.12

# Auth / Security
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
pyotp==2.9.0

# Reporting
reportlab==4.2.5

# EXIF
exifread==3.0.0

# HuggingFace (AI Ensemble models)
transformers>=4.36.0

# Data Validation
pydantic==2.10.4
pydantic-settings==2.7.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

# Utilities
python-dotenv==1.0.1
aiofiles==24.1.0
